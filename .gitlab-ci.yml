image: maven:latest

stages:
  - build

build-project:
    stage: build
    script:
        - echo "Maven package start"
        - mvn install -D skipTests
        - echo "Maven package done"
    artifacts:
      paths:
        - "/target"