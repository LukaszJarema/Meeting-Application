image: maven:latest

stages:
  - build

build-project:
    stage: build
    script:
        - echo "Maven package start"
        - mvn package -B
        - echo "Maven package done"
    artifacts:
      paths:
        - "/target"